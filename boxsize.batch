#!/bin/bash
#SBATCH -N 5
#SBATCH --tasks-per-node=64
#SBATCH --cpus-per-task=2
#SBATCH --array=1-5
#SBATCH -J boxsize
#SBATCH -t 48:00:00
#SBATCH -p RM

#SBATCH --mail-type=ALL
#SBATCH -o %x-%j.out
#SBATCH -e %x-%j.err

module load gromacs/2020.2-cpu

cd $PROJECT/visc

set -x
set echo

mpirun -np $SLURM_NPROCS gmx_mpi mdrun -pin on -deffnm r$SLURM_ARRAY_TASK_ID/step7_boxsize -v -maxh 47 -dlb yes -npme 60
