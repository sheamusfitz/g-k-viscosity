#!/bin/bash
#SBATCH -J eq            # Name of job; user chosen
#SBATCH -p RM-shared           # queue: always use skx-normal
#SBATCH -N 1                    # number of nodes
#SBATCH --ntasks-per-node 1
#SBATCH --array=1-5
#SBATCH -t 07:00:00

#SBATCH --mail-type=all
#SBATCH -o %x-%j.out
#SBATCH -e %x-%j.err

module load gromacs/2020.2-cpu

set -x
set echo

cd $SLURM_SUBMIT_DIR

cd $PROJECT/visco-fluc/test-psm/r$SLURM_ARRAY_TASK_ID
csh README
